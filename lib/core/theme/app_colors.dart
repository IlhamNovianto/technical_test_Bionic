import 'package:flutter/material.dart';

class AppColors {
  // ── Primary ─────────────────────────────────────────────────
  static const primary = Color(0xFF1A73E8);
  static const primaryLight = Color(0xFF4A9EF5);
  static const primaryDark = Color(0xFF0D47A1);

  // ── Light Mode ──────────────────────────────────────────────
  static const lightBackground = Color(0xFFF5F5F5);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightAppBar = Color(0xFFFFFFFF);
  static const lightTextPrimary = Color(0xFF1A1A1A);
  static const lightTextSecondary = Color(0xFF757575);
  static const lightTextHint = Color(0xFFBDBDBD);
  static const lightDivider = Color(0xFFEEEEEE);
  static const lightIcon = Color(0xFF424242);
  static const lightInputFill = Color(0xFFF5F5F5);
  static const lightChatUser = Color(0xFF1A73E8);
  static const lightChatBot = Color(0xFFF1F1F1);
  static const lightChatUserText = Color(0xFFFFFFFF);
  static const lightChatBotText = Color(0xFF1A1A1A);
  static const lightOfflineBanner = Color(0xFFFFF3E0);
  static const lightOfflineText = Color(0xFFE65100);
  static const lightSourceBadge = Color(0xFFE8F0FE);
  static const lightSourceText = Color(0xFF1A73E8);
  static const lightShadow = Color(0x0F000000);

  // ── Dark Mode ───────────────────────────────────────────────
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkCard = Color(0xFF2C2C2C);
  static const darkAppBar = Color(0xFF1E1E1E);
  static const darkTextPrimary = Color(0xFFE0E0E0);
  static const darkTextSecondary = Color(0xFF9E9E9E);
  static const darkTextHint = Color(0xFF616161);
  static const darkDivider = Color(0xFF2C2C2C);
  static const darkIcon = Color(0xFFE0E0E0);
  static const darkInputFill = Color(0xFF2C2C2C);
  static const darkChatUser = Color(0xFF1565C0);
  static const darkChatBot = Color(0xFF2C2C2C);
  static const darkChatUserText = Color(0xFFFFFFFF);
  static const darkChatBotText = Color(0xFFE0E0E0);
  static const darkOfflineBanner = Color(0xFF3E2723);
  static const darkOfflineText = Color(0xFFFFCCBC);
  static const darkSourceBadge = Color(0xFF1A3A6B);
  static const darkSourceText = Color(0xFF90CAF9);
  static const darkShadow = Color(0x00000000);
}
